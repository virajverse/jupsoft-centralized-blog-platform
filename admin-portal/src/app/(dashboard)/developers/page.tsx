import React from 'react';
import { ApiPortalView } from '../../../components/developers/ApiPortalView';

export const metadata = {
  title: 'Developer API & Integration Portal | Jupsoft CMS',
  description: 'Interactive REST API tester, Next.js 16 App Router code generator, and on-demand ISR revalidation webhooks.',
};

export default function DevelopersPage() {
  return <ApiPortalView />;
}
